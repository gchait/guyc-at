{
	servers {
		protocols h1 h2
	}
}

{$SITE_ADDRESS} {
	encode gzip
	
	handle /api/* {
		uri strip_prefix /api
		reverse_proxy {$API_HOST}
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
